%div 利用者登録
= form_with url: company_users_path(current_company), local: true do |f|
  = f.label :name
  = f.text_field :name
  = f.label :password
  = f.password_field :password
  = f.label :password_confirmation
  = f.password_field :password_confirmation
  = f.submit
